HANDOFF PACKET — Circle Description UI (Host-Only Edit)
================================================================================

SUMMARY
-------
Added description field to circles with display on cards and editable in Group Settings.
Host-only editing enforced via frontend UI gating and backend 403 handling.

PRECHECK
--------
✅ pwd: /Users/paulsmbp/Documents/GitHub/open-invite-app
✅ remote: https://github.com/paulyd-oi/open-invite-app.git
✅ branch: main
✅ status: clean

CHANGES
-------
1. shared/contracts.ts — Added `description: z.string().nullable().optional()` to circleSchema
2. src/components/CircleCard.tsx — Display description under name (max 2 lines, muted text)
3. src/app/circle/[id].tsx — Group Settings description section with host-only edit

KEY DIFFS
---------
contracts.ts:
  + description: z.string().nullable().optional(),

CircleCard.tsx:
  + {circle.description && (
  +   <Text ... numberOfLines={2}>{circle.description}</Text>
  + )}
  + Changed member count from "text-sm" to "text-xs" to make room for description

circle/[id].tsx:
  + const [editingDescription, setEditingDescription] = useState(false);
  + const [descriptionText, setDescriptionText] = useState("");
  + const isHost = circle?.createdById === session?.user?.id;
  + updateCircleMutation with PUT /api/circles/:id
  + Description section in Group Settings modal with:
    - Read mode: shows description or "No description"
    - Edit mode (host only): TextInput with 160 char limit + Save/Cancel
    - 403 error handling with friendly toast

TEST PLAN (TestFlight steps)
----------------------------
1. Friends tab → Planning section → Verify circle cards show description if set
2. Tap circle → Tap member avatars → Group Settings
3. Verify description section shows:
   - If host: "Tap Edit to add a description" or existing description + Edit button
   - If member: "No description" or existing description (no Edit button)
4. As host: Tap Edit → Enter description (up to 160 chars) → Save
5. Verify toast "Description updated" appears
6. Verify description shows on card and in settings
7. As host: Clear description → Save → Verify clears to "No description"
8. As member: Open settings → Verify no Edit button visible

RISKS / FOLLOWUPS
-----------------
- Backend must return description field (confirmed live)
- Backend enforces 403 for non-host updates
- No emoji picker for description (text only)

PUSH STATUS
-----------
✅ Commit: bb5b2ea
✅ Pushed to origin/main
================================================================================

