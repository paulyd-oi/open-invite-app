HANDOFF PACKET — Discover Card Attendee Polish V3
Date: 2026-03-21

SUMMARY

Removed "N friend(s) going" text label from Discover card attendee area. Replaced SocialProof component with inline EntityAvatar stack rendering visual-only overlapping profile circles and a compact +N badge. No text labels remain.

EXACT FILES CHANGED

1. src/app/discover.tsx
   - Removed SocialProof import (unused after this change)
   - Replaced SocialProof component call with inline EntityAvatar stack
   - joinRequests filtered to accepted, sliced to 3, rendered as overlapping circles
   - +N badge renders only when remaining > 0 (accent-colored circle, white text)
   - "Be first" text removed (returns null for count=0)

FIX APPLIED

- Removed: SocialProof component (which rendered "1 friend going" / "Alice + 2 others going" copy)
- Added: Inline avatar stack using EntityAvatar with real profile photos from joinRequests
  - 20px circles, -6px overlap, 1.5px border matching plaqueBg
  - Accent-tinted fallback backgrounds when no photo
  - +N badge: 20px accent circle with white bold count text
  - z-index stacking: leftmost avatar on top
- Zero attendees: renders nothing (was "Be first" text)

WHAT WAS NOT CHANGED

- Card layout structure, image zone, content panel, footer row
- Description row, date/time metadata styling
- Save/bookmark behavior
- Category pill, urgency chip, availability pill
- Card tap navigation
- Event contracts, backend, other screens

VERIFICATION

npm run typecheck: PASS (0 errors)
bash scripts/ai/verify_frontend.sh: same pre-existing failures only (create.tsx:1571, circle/[id].tsx:2166 — untouched files)

GIT PROOF

git show --stat HEAD
commit 3edc8bc675f360a0ab08b6226cbf3216b4147040
 src/app/discover.tsx | 58 ++++++++++++++++++++++++++++++++++++++++------------
 1 file changed, 45 insertions(+), 13 deletions(-)

git show --name-only HEAD
src/app/discover.tsx

FINAL STATUS

DISCOVER ATTENDEE POLISH APPLIED — NEEDS DESIGN REVIEW
