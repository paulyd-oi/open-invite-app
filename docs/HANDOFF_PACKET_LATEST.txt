HANDOFF PACKET — DISABLE APPLE SIGN-IN FOR LAUNCH
====================================================================
Date: 2026-02-01
Branch: main
Repo: github.com/paulyd-oi/open-invite-app.git

SUMMARY
-------
Temporarily disabled Apple Sign-In UI + handler for launch build.
Email login remains fully functional. Apple code preserved (not deleted).

FILES CHANGED
-------------
1. src/app/welcome.tsx
   - Added APPLE_SIGNIN_ENABLED = false feature flag
   - Apple button no longer renders (flag guards condition)
   - "or" divider no longer renders (flag guards condition)
   - handleAppleSignIn cannot be triggered (no button to call it)

KEY DIFFS
---------
After Apple import block:
+ // Feature flag: disable Apple Sign-In for launch build
+ // Set to true to re-enable, or wire to EXPO_PUBLIC_ENABLE_APPLE_SIGNIN
+ const APPLE_SIGNIN_ENABLED = false;

Apple button render:
- {Platform.OS === "ios" && isAppleSignInReady && AppleAuthentication && (
+ {APPLE_SIGNIN_ENABLED && Platform.OS === "ios" && isAppleSignInReady && AppleAuthentication && (

"or" divider render:
- {Platform.OS === "ios" && isAppleSignInReady && (
+ {APPLE_SIGNIN_ENABLED && Platform.OS === "ios" && isAppleSignInReady && (

COMMANDS RUN + RESULTS
----------------------
npm run typecheck                    PASS

RUNTIME STATUS
--------------
- Typecheck: PASS
- No new dependencies
- appleSignIn.ts preserved (not deleted)
- Navigation architecture unchanged
- Icon system untouched

WHAT TO TEST
------------
1. Launch app on iOS device/simulator
2. Verify welcome screen loads
3. Verify NO Apple Sign-In button visible
4. Verify NO "or" divider visible
5. Enter email + password
6. Tap Sign In / Sign Up
7. Verify email login flow works end-to-end

TO RE-ENABLE APPLE SIGN-IN
--------------------------
Change in src/app/welcome.tsx:
  const APPLE_SIGNIN_ENABLED = true;
