HANDOFF_PACKET — Image Picker Latency + ONBOARD_AVATAR Log Cleanup
Date: 2026-03-20

SUMMARY

Reduced perceived latency when tapping Add Photo during onboarding avatar selection. Moved media library permission request from on-tap to slide 3 mount so it resolves before the user ever taps. handlePickPhoto now uses the cached permission result, skipping the async permission call entirely when already granted. Also removed all 28 [ONBOARD_AVATAR] diagnostic logs across 3 files — the upload bug they were tracking is fixed and shipped.

WHAT CHANGED

1. src/app/welcome.tsx
   - Added mediaPermissionRef (useRef) and useEffect that pre-requests media library permission when currentSlide === 3
   - Rewrote handlePickPhoto: checks cached permission first, only falls back to fresh request if not granted. Added [IMAGE_PICKER_PERF] DEV-only timing logs (permission resolve + picker return)
   - Removed 12 [ONBOARD_AVATAR] console.log calls from handlePickPhoto and uploadPhotoInBackground
   - uploadPhotoInBackground: stripped verbose diagnostic logs, kept functional error handling

2. src/lib/imageUpload.ts
   - Removed 14 [ONBOARD_AVATAR] console.log calls from uploadByKind (A1-A4, B1-B3, C1-C3, D1-D4, E1)
   - Zero functional changes — all error paths and throw statements preserved

3. src/lib/authClient.ts
   - Removed 2 [ONBOARD_AVATAR] console.log calls from $fetch (AUTH token validation + cookie header logging for upload paths)
   - Existing AUTH_DEBUG logging remains untouched

WHY THIS HELPS

Before: Every Add Photo tap awaited requestMediaLibraryPermissionsAsync() before launching picker (~200-500ms on first call, ~50-100ms on subsequent). After: Permission is already cached from slide mount. Tap goes straight to launchImageLibraryAsync.

VERIFICATION

npx tsc --noEmit: PASS clean (exit 0)
bash scripts/ai/verify_frontend.sh: same pre-existing failures only
  create.tsx:1571 and circle/[id].tsx:2166 raw Image annotations
  No new failures introduced.
Zero [ONBOARD_AVATAR] matches in src/ confirmed via grep.

BLAST RADIUS

Non-avatar upload flows (event photos, circle photos, banners): UNAFFECTED. Only removed DEV-only logs, no functional changes.
Permission behavior: IDENTICAL. Same requestMediaLibraryPermissionsAsync call, just moved earlier. If permission not yet resolved on tap, falls back to fresh request.
Upload error handling: PRESERVED. All try/catch blocks and throw statements kept intact.

FINAL STATUS

DONE — NEEDS RUNTIME RETEST (confirm picker opens faster on Add Photo tap)
