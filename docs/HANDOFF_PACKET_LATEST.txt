HANDOFF PACKET — Discover Card Redesign V1
Date: 2026-03-21

SUMMARY

Redesigned Discover tab event card layout. Moved save/bookmark from image overlay to themed content panel. Added attendee avatar cluster to footer. Moved category pill to image top-left. Content area uses event theme surface token (plaqueBg). Minimal diff — inline card rendering only.

EXACT CHANGES

1. Image zone:
   - Category pill (theme label) → top-left overlay with dark translucent bg
   - Urgency chip → below category pill (stacks vertically)
   - LinearGradient vignette REMOVED (was overlaying bottom of image)

2. Content panel (plaqueBg background):
   - TOP ROW: Title + Bookmark save button (replaced Heart icon on photo)
   - Bookmark in 32px circle, accent-tinted when saved (STATUS.interested)
   - LOCATION ROW: MapPin + location text
   - FOOTER ROW (separated by 1px border):
     - Left: Calendar icon (accent-colored) + date/time + availability chip
     - Right: Attendee avatar cluster (overlapping circles) + count + momentum label

3. Attendee cluster:
   - Up to 3 overlapping circles (22px, accent-tinted backgrounds with decreasing opacity)
   - Users icon inside each circle
   - Count text: "{N} going" or "Be first" when 0
   - Momentum labels: "Almost full" (≤3 spots), "Full" (at capacity), "Popular" (≥10)

4. Card container:
   - borderRadius: 20 (was 16)
   - Image aspectRatio: 1.9
   - Card background: plaqueBg (event theme token)
   - Accent border: cardAccent with 25 alpha

EXACT FILE CHANGED

1. src/app/discover.tsx
   - Inline renderItem card layout (~lines 615-860)
   - No new imports, no new components, no new files

WHAT WAS REMOVED

- LinearGradient vignette overlay on image bottom
- Heart icon for save (replaced with Bookmark)
- Theme label pill in content area (moved to image overlay)
- Host avatar row from content area

VERIFICATION

npm run typecheck: PASS (0 errors)
bash scripts/ai/verify_frontend.sh: same pre-existing failures only (create.tsx:1571, circle/[id].tsx:2166 — untouched files)

BLAST RADIUS

- ONLY discover.tsx touched — no other screens or components affected
- No new dependencies, no new files
- All existing imports reused (Bookmark, Calendar, MapPin, Users from @/ui/icons)
- plaqueBg/cardAccent tokens already computed in scope — no new theme logic

FINAL STATUS

COMPLETE
