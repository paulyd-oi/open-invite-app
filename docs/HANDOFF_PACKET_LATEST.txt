HANDOFF PACKET — refetchOnMount: false Systemic Sweep V1
Date: 2026-03-21

SUMMARY

Removed refetchOnMount: false from 8 TanStack Query hooks across 4 files. This anti-pattern prevented stale data from refreshing when users navigate between tabs. Same root cause already fixed on Discover and friend requests in prior tasks. All queries retain their staleTime (30s-5min) and placeholderData where present. TQ default (refetchOnMount: true) now allows stale data to refetch on mount while keeping cached data visible.

REMOVALS

- src/app/friends.tsx:1011 — query key: circleKeys.all(), staleTime: 5min, placeholderData: yes
- src/app/friends.tsx:1064 — query key: ["pinnedFriendships"], staleTime: 5min, placeholderData: no
- src/app/social.tsx:905 — query key: eventKeys.feedPaginated(), staleTime: 30s, placeholderData: yes
- src/app/social.tsx:954 — query key: eventKeys.mine(), staleTime: 1min, placeholderData: yes
- src/app/social.tsx:972 — query key: eventKeys.attending(), staleTime: 1min, placeholderData: yes
- src/app/social.tsx:1117 — query key: ["friends"], staleTime: 5min, placeholderData: yes
- src/app/calendar.tsx:1441 — query key: ["friends"], staleTime: 5min, placeholderData: no
- src/lib/usePaginatedFriends.ts:75 — query key: qk.friend.list(search), staleTime: 5min, placeholderData: no

KEPT INTENTIONALLY

- src/app/_layout.tsx:319 — auth/session query uses explicit invalidation on login/logout; refetchOnMount: false prevents redundant auth checks on every screen mount
- src/components/AnnouncementBanner.tsx:70 — low-frequency banner content, intentionally cached for session duration
- src/components/UpdateBanner.tsx:57 — low-frequency update check, intentionally cached for session duration
- src/app/discover.tsx:191,201,212 — already fixed on fix/discover-truth-sync-v1 branch (not yet merged to main)
- src/app/friends.tsx:866 — already fixed on fix/friend-request-wiring-v1 branch (not yet merged to main)

VERIFICATION

npm run typecheck: PASS (0 errors)
bash scripts/ai/verify_frontend.sh: same pre-existing failures only (create.tsx:1571, circle/[id].tsx:2166 — untouched files)
grep refetchOnMount: false — remaining instances match exclusion list exactly

GIT PROOF

git show --stat HEAD
(see below after commit)

git show --name-only HEAD
(see below after commit)

FINAL STATUS

FIXED
