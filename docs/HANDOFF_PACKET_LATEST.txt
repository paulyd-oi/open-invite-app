HANDOFF PACKET — Cache Invalidation Gaps V1
Date: 2026-03-21

SUMMARY

Fixed 5 cross-surface cache invalidation and lifecycle gaps identified in docs/SURFACE_AUDIT_REPORT.txt. These caused stale data on Discover, Social, and Activity feeds after RSVP, event CRUD, and friend mutations.

GAP 1 FIX: src/lib/eventQueryKeys.ts
- Added eventKeys.feedPopular() to 5 helpers:
  getInvalidateAfterRsvpJoin, getInvalidateAfterRsvpLeave,
  getInvalidateAfterEventCreate, getInvalidateAfterEventEdit,
  getInvalidateAfterEventDelete
- feedPopular() now sits alongside feed() and feedPaginated() in every helper
- No existing keys removed

GAP 2 FIX: src/app/discover.tsx
- Replaced saveMutation ad-hoc invalidation (2 keys: feedPopular + myEvents) with
  SSOT call: invalidateEventKeys(queryClient, getInvalidateAfterRsvpJoin(eventId), "discover_save")
- SSOT helper covers 10 keys: single, attendees, interests, rsvp, feed, feedPaginated,
  feedPopular, myEvents, calendar, attending
- Kept setSavedEvents optimistic update, haptics, and toast — only invalidation calls replaced
- Added imports: invalidateEventKeys, getInvalidateAfterRsvpJoin from eventQueryKeys

GAP 3 FIX: src/lib/refreshAfterMutation.ts → refreshAfterFriendAccept
- Added 3 event feed keys: eventKeys.feed(), eventKeys.feedPaginated(), eventKeys.feedPopular()
- New friend's events now propagate to Social tab and Discover after accept
- All existing keys preserved (friendKeys.all, requests, suggestions, pinned, circleKeys.all)

GAP 4 FIX: src/lib/refreshAfterMutation.ts → refreshAfterFriendAccept + refreshAfterFriendReject
- Added ["notifications"] key to both helpers
- friend_request notification now cleared from Activity feed after accept or reject
- All existing keys preserved

GAP 5 FIX: src/app/social.tsx → discoveryEvents memo
- Added past/ended event filter before existing filters (cheap early return)
- Pattern: endTime ?? startTime < Date.now(), with Number.isNaN guard for invalid dates
- Added excludedExpiredCount to [P0_SOCIAL_OPEN_INVITES_FILTER] DEV log
- Same proven pattern as Task 4 profile fix

VERIFICATION

npm run typecheck: PASS (0 errors)
bash scripts/ai/verify_frontend.sh: same pre-existing failures only (create.tsx:1571, circle/[id].tsx:2166)

BLAST RADIUS
- eventQueryKeys.ts: 5 helpers modified (feedPopular added to each)
- refreshAfterMutation.ts: 2 helpers modified (event feeds + notifications added)
- discover.tsx: 1 mutation modified (ad-hoc → SSOT)
- social.tsx: 1 memo modified (expiry filter added)
- No UI, layout, or component changes
- No existing invalidation keys removed
- feedPopular() now appears in all 5 event invalidation helpers ✓
- refreshAfterFriendAccept now includes event feed + notification keys ✓
- refreshAfterFriendReject now includes notification keys ✓
- discoveryEvents memo now filters past events ✓

GIT PROOF

(see below after commit)

FINAL STATUS

FIXED
